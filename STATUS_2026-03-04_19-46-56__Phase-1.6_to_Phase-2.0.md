# 状态与下一步 Todo（Phase-1.6_to_Phase-2.0）

时间戳：2026-03-04_19-46-56
文件阶段：Phase-1.6_to_Phase-2.0

## 当前状态

- 已完成：
  - 任务并发执行（DAG 模板 + asyncio 调度）
  - 决策分级（L0-L3）与 Todo 票据
  - checkpoint 回退
  - 可视化控制面原型（/ui）
  - SQLite 持久化（JSON 快照 + 结构化表）
  - 分页查询 API（任务与历史）
  - 容量/查询耗时报告脚本（tools/capacity_report.py）

- 当前版本定位：
  - Phase 1.6（R1 过渡版）
  - 可运行、可观察、可回退，具备后续扩展底座

## 当前限制

- 仍是单机单进程，不支持分布式调度。
- 主存储仍为 SQLite，不适合更高并发写入。
- 向量检索尚未接入。
- metrics 专用数据存储（ClickHouse）尚未接入。

## 下一步 Todo（立即执行）

1. PostgreSQL 迁移脚手架
- 建立 DDL/migration 文件。
- 定义 repository 抽象接口，避免 runtime 直接耦合 SQLite。
- 增加 Postgres store 的最小实现骨架。

2. pgvector 接入准备
- 规划记忆向量表结构（embedding + metadata + user/task 过滤）。
- 预留检索 API 接口。

3. metrics 存储解耦
- 把 metrics 查询接口从“内存计算”逐步切换为“可插拔后端”。
- 为 ClickHouse 写入器预留事件 schema。

## 验收标准（本阶段）

- 工程层具备 SQLite/PostgreSQL 双后端切换能力（至少代码结构已就绪）。
- 存储层接口稳定，后续接 pgvector/ClickHouse 不需要重写业务逻辑。
- 文档与代码保持同步，运行说明可复现。
